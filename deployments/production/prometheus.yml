global:
  scrape_interval: 2s
  evaluation_interval: 2s

scrape_configs:
  - job_name: faultplane_production_telemetry
    static_configs:
      - targets:
          - localhost:8080
    metrics_path: /api/v1/metrics/prometheus
