# FaultPlane Troubleshooting

This document covers common installation, startup, BPF, permission,
kernel, and WSL2 issues when running FaultPlane.

FaultPlane is beta software. Test it in a controlled environment before
using it with production workloads.

---

## 1. Installer Fails

Run the installer from the FaultPlane directory:

```bash
sudo ./install.sh
